export const environment = {
  production: true,
  /**
   * URL base del backend (incluye el prefijo /api).
   * En producción se sirve tras el mismo dominio; cámbialo si el backend
   * vive en otro host (p. ej. 'https://api.tu-dominio.com/api').
   */
  apiUrl: '/api',
};
